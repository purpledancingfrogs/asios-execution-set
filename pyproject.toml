[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "asios-execution-set"
version = "0.1.0"
description = "Deterministic ASIOS execution set"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["decision_decomposition"]
